{
  "name": "kurkarurka",
  "version": "0.1.0",
  "private": true,
  "description": "Kurkarurka — gra TypeScript + Matter.js kompilowana do jednego offline'owego pliku HTML",
  "type": "module",
  "scripts": {
    "dev": "node build.mjs --watch",
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "matter-js": "0.19.0"
  },
  "devDependencies": {
    "@types/matter-js": "^0.19.8",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
